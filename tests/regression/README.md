# Regression tests

Promote cases from `eval/cases/combined_hard_cases.jsonl` into executable regression tests once:
- the dataset slice is fixed,
- the expected result is reviewed, and
- the case is known to represent a real failure mode.

Do not remove or weaken a locked regression case only to improve score.
